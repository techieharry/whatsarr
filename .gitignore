# dependencies + build
node_modules/
dist/
*.tsbuildinfo

# bot state — credentials, db, conversation history
auth_info_baileys/
auth_info_baileys.*/
data/
*.sqlite
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal

# secrets
.env
.env.local
.env.*.local

# per-deployment routing (real library root folders + Sonarr/Radarr profile ids).
# ship routing.config.example.json; keep your real routing.config.json out.
routing.config.json

# logs
logs/
*.log

# scratch / generated by tools
qr.html
groups.txt
.stversions/

# OS
.DS_Store
Thumbs.db
